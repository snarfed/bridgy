application: brid-gy
version: 1
runtime: python27
threadsafe: false
api_version: 1
default_expiration: 1d

builtins:
- remote_api: on

libraries:
- name: ssl
  version: latest

handlers:

# static
- url: /static
  static_dir: static

- url: /favicon.ico
  static_files: static/favicon.ico
  upload: static/favicon.ico

# dynamic
- url: /webmention
  script: webmention.application

- url: /facebook/.*
  script: facebook.application
  login: required

- url: /googleplus/.*
  script: googleplus.application
  login: required

- url: /wordpress/.*
  script: wordpress.application
  login: required

- url: /twitter/.*
  script: twitter.application
  login: required

- url: /oauth2callback
  script: oauth2client/appengine.application

- url: /_ah/queue/.*
  script: tasks.application
  login: admin

- url: .*
  script: app.application
  # not secure: always because i haven't bought an SNI slot for SSL on the
  # brid.gy custom domain.
  # secure: optional

skip_files:
- ^(.*/)?.*\.py[co]
- ^(.*/)?.*/RCS/.*
- ^(.*/)?\..*
- ^(.*/)?.*\.bak$
- ^(.*/)?(debian|doc|docs|pydocs|python3|ref|samples|tests)/.*

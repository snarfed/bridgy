# queue handlers are defined in tasks.py
queue:
- name: poll
  max_concurrent_requests: 1
  retry_parameters:
    min_backoff_seconds: 120

- name: propagate
  rate: 12/m  # one every 5s
  retry_parameters:
    task_retry_limit: 30
    task_age_limit: 3d
    min_backoff_seconds: 30

service: background
# version: 1
runtime: python37
entrypoint: gunicorn -b :$PORT tasks:application

instance_class: B1
basic_scaling:
  max_instances: 1

handlers:

- url: .*
  script: auto
  secure: always
